<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- システムプロパティから設定値を読み込み -->
    <property name="LOG_DIR" value="${log.dir:-./logs}" />
    <property name="LOG_LEVEL" value="${log.level:-INFO}" />
    
    <!-- コンソール出力設定 -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>
        
    <!-- ファイル出力設定 -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_DIR}/gxp21-amplification-data.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>${LOG_DIR}/gxp21-amplification-data.%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <timeBasedFileNamingAndTriggeringPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedFNATP">
                <maxFileSize>100MB</maxFileSize>
            </timeBasedFileNamingAndTriggeringPolicy>
            <maxHistory>30</maxHistory>
            <totalSizeCap>3GB</totalSizeCap>
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>
    
    <!-- ルートロガー設定 -->
    <root level="${LOG_LEVEL}">
        <appender-ref ref="FILE" />
        <appender-ref ref="CONSOLE" />
    </root>
    
    <!-- Hibernateのログレベルを調整 -->
    <logger name="org.hibernate" level="WARN" />
    <logger name="org.hibernate.SQL" level="INFO" />
    
    <!-- AWSのログレベルを調整 -->
    <logger name="software.amazon.awssdk" level="WARN" />
</configuration>