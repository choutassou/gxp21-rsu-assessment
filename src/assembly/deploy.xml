<assembly xmlns="http://maven.apache.org/ASSEMBLY/2.1.3"
          xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
          xsi:schemaLocation="http://maven.apache.org/ASSEMBLY/2.1.3
                              http://maven.apache.org/xsd/assembly-2.1.3.xsd">
    <id>deploy</id>
    <formats>
        <format>zip</format>
    </formats>
    <includeBaseDirectory>false</includeBaseDirectory>
    
    <fileSets>
        <!-- 実行可能JARファイル -->
        <fileSet>
            <directory>${project.build.directory}</directory>
            <outputDirectory>lib</outputDirectory>
            <includes>
                <include>${project.artifactId}-${project.version}.jar</include>
            </includes>
        </fileSet>
        
        <!-- 設定ファイル -->
        <fileSet>
            <directory>${project.basedir}</directory>
            <outputDirectory>config</outputDirectory>
            <includes>
                <include>Assessment.properties</include>
            </includes>
        </fileSet>
        
        <!-- 起動スクリプト -->
        <fileSet>
            <directory>${project.basedir}/src/scripts</directory>
            <outputDirectory>bin</outputDirectory>
            <includes>
                <include>**/*</include>
            </includes>
            <fileMode>0755</fileMode>
        </fileSet>
        
        <!-- ログディレクトリ作成用 -->
        <fileSet>
            <directory>${project.basedir}/src/scripts</directory>
            <outputDirectory>logs</outputDirectory>
            <excludes>
                <exclude>**/*</exclude>
            </excludes>
        </fileSet>
        
        <!-- データディレクトリ作成用 -->
        <fileSet>
            <directory>${project.basedir}/src/scripts</directory>
            <outputDirectory>data</outputDirectory>
            <excludes>
                <exclude>**/*</exclude>
            </excludes>
        </fileSet>
    </fileSets>
</assembly>